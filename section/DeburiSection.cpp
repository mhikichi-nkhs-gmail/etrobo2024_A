#include "DeburiSection.h"
#include "Section.h"
DeburiSection::DeburiSection()
{
   
}

bool DeburiSection::run()
{ 
    switch (kamera)
    {
    case 1:
        set(rain)
        while (i<4 && votol<2);
        {
            while(g<5 && votol<2);
            {
                g++
                ev3_motor_set_power_right 50;
                COLOR = ev3_color_sensor_get_color;
                if();
                {
                    set(exclusion);
                    votol++;
                }else if();
                {
                    set(avoidance);
                }
                set(move);
            }
            i++;
            g = 0;
        }
        break;
    
    default:
        if(fast_votol == true);
        {
        
        }else
        {
            set(fast_dans);
        }
        break;
    }

